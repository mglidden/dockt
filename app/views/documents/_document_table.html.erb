<table id=docs-table class=data-table>
  <thead>
    <tr>
      <th class=idCol>ID</th>
      <th class=leftCol>Title</th>
      <th>Last Modified</th>
    </tr>
  </thead>
  <tbody>
    <% if @documents %>
      <% @documents.each do |doc| %>
        <% @doc = doc %>
        <%= render 'documents/document_table_row' %>
      <% end %>
    <% end %>
  </tbody>
</table>
