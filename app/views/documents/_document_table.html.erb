<div class=card id=documents>
  <table id=docs-table class=data-table>
    <tr>
      <th class=idCol>ID</th>
      <th>Title</th>
      <th>URL</th>
    </tr>
    <% if @documents %>
      <% @documents.each do |doc| %>
        <tr>
          <td class=idCol><%= doc.id %></td>
          <td><%= doc.title%></td>
          <td><%= doc.url %></td>
        </tr>
      <% end %>
    <% end %>
  </table>
</div>

