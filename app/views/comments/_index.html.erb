<table>
  <tr>
    <th>Commenter</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @comments.each do |comment| %>
    <tr>
      <td><%= comment.commenter %></td>
      <td><%= comment.body %></td>
      <td><%= link_to "Show", group_document_comment_path(@group, @document, comment) %></td>
      <td></td>
      <td></td>
    </tr>
  <% end %>
</table>
