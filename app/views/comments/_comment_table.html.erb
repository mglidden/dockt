<table id=comments-table class=data-table>
  <tr>
    <th class=idCol>ID</th>
    <th class=leftCol>Commenter</th>
    <th>Body</th>
  </tr>
  <% if @comments %>
    <% @comments.each do |comment| %>
      <tr class=table_row>
        <td class=idCol><%= comment.id %></td>
        <td class=leftCol><%= comment.commenter %></td>
        <td><%= comment.body %></td>
      </tr>
    <% end %>
  <% end %>
</table>
