<%= form_for(@group, :remote=>true) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h2>Create a new group</h2>

  <div class="field">
    <%= f.text_field :name, :value => 'Group name', :onfocus => 'util.removeText(\'#group_name\', \'Group name\')', :onblur => 'util.addText(\'#group_name\', \'Group name\')'%>
  </div>
  <div class="actions" onclick=toolbar.close()>
    <%= f.submit %>
  </div>
<% end %>
